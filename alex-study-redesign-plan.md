# Alex Study — تشخيص كامل + خطة إعادة بناء

راجعت الكود بعمق أكتر (CSS tokens، الأزرار، الأيقونات، الوضع الليلي، الـ spacing) بعد أول مراجعة. النتيجة: المشكلة مش بس "حس عام" — فيه **أعطال حقيقية وتناقضات تصميمية موثّقة بالسطر**. القائمة دي أكبر وأخطر مما كنت متوقع.

---

## ملخص تنفيذي

| # | المشكلة | الخطورة |
|---|---------|---------|
| ١ | نظام الوضع الليلي/النهاري **معطّل بالكامل** (زرار بيدوّس ومفيش تأثير فعلي) | 🔴 عطل حقيقي |
| ٢ | خطوط النظام (`--font-geist-sans/mono`) غير معرّفة نهائيًا | 🔴 عطل حقيقي |
| ٣ | متغيرات ألوان مستخدمة وغير موجودة (`--text-muted`, `--accent-base`) | 🔴 عطل حقيقي |
| ٤ | **١٤٤ كلاس CSS معرّف أكتر من مرة** في نفس الملف | 🔴 مصدر تعارضات مستمر |
| ٥ | عناوين بحجم صفحة هبوط (hero) متسحوبة على صفحات داخلية | 🟠 تصميمي |
| ٦ | لا يوجد نظام أزرار موحّد — ٨+ أنواع مختلفة + أزرار خام بدون تصميم | 🟠 تصميمي |
| ٧ | الأيقونات: مكتبة Lucide في السايدبار بس، والباقي كله رموز يونيكود خام (↑ ↓ ⠿) | 🟠 تصميمي |
| ٨ | نصف قطر الزوايا (border-radius) عشوائي في ٥٠ مكان تقريبًا، ٣ بس منهم بيستخدموا التوكن الرسمي | 🟠 تصميمي |
| ٩ | ألوان hex مكتوبة يدويًا بره نظام التوكنز (١٨ مكان) | 🟡 تقني |
| ١٠ | مسافات (gap/padding) بقيم عشوائية بلا نظام (١٥+ قيمة مختلفة بدل مقياس ثابت) | 🟡 تصميمي |
| ١١ | القايمة الجانبية والـ Bottom Nav نظامين منفصلين بيكرروا نفس الروابط | 🟠 تصميمي |
| ١٢ | next-intl مركّب ومش مستخدم — ترجمة يدوية متكررة في كل مكان | 🟠 هيكلي |
| ١٣ | Zustand و TanStack Query متسطبين ومش مستخدمين — تكرار منطق يدوي | 🟠 هيكلي |

النقط ١-٣ مش مسائل ذوق — دي أعطال حقيقية بتخلي فيتشرز موجودة في الكود ما بتشتغلش خالص. خلّيني أوريك بالتفصيل.

---

## تفاصيل الاكتشافات الجديدة

### ١. زرار تبديل الوضع الليلي معطّل بالكامل (باج حقيقي)

في `app-shell.tsx` و `settings-workspace.tsx` بيتحط attribute اسمه `data-theme="dark"` على `<html>` لما المستخدم يختار Dark يدويًا:

```js
document.documentElement.dataset.theme = theme.toLowerCase();
```

لكن لما دورت في كل ملفات الـ CSS على أي حاجة بتستخدم `[data-theme="dark"]`... **مفيش ولا سطر واحد**. الوضع الليلي في الكود كله شغّال بـ:

```css
@media (prefers-color-scheme: dark) { :root { ... } }
```

يعني الألوان بتتغير بس حسب إعداد نظام التشغيل نفسه، مش حسب اختيار المستخدم جوه التطبيق. فلو حد فتح إعدادات الموقع واختار "الوضع الداكن" يدويًا، **مفيش أي تأثير خالص** إلا لو جهازه أصلاً على وضع داكن من قبل — والعكس، لو اختار "فاتح" يدويًا وجهازه على داكن، بردو مفيش تأثير. الفيتشر ده موجود في الواجهة ومتخزن في قاعدة البيانات (`/api/me/preferences`) بس بصراحة **لا يعمل شيئًا** بصريًا.

### ٢. الخطوط الأساسية غير معرّفة (باج حقيقي، مش بس اختيار سيء)

```css
/* tokens.css */
--font-sans: var(--font-geist-sans);
--font-mono: var(--font-geist-mono);
```

`--font-geist-sans` و `--font-geist-mono` مش معرّفين في أي مكان في المشروع (الخط الوحيد المحمّل فعليًا هو Cinzel، عبر `next/font/local`). في CSS، لو الـ variable غير معرّف وبدون fallback، القيمة كلها بتبقى "invalid" والمتصفح بيرجع للخط الموروث/الافتراضي — يعني عناصر كتير (زي أرقام التايمر، الخط الأساسي) ممكن تظهر بخط عشوائي مش اللي المصمم قصده، وده بيفسّر الإحساس إن الخط "مش متناسق" بين الصفحات.

### ٣. متغيرات ألوان مستخدمة وهي مش موجودة

جوه الداشبورد:
```jsx
style={{ color: "var(--text-muted)" }}
style={{ color: "var(--accent-base)" }}
```
لكن `tokens.css` عنده بس `--muted` و `--accent` (أسماء مختلفة). النتيجة: النص ده بيظهر بلون غير مقصود.

### ٤. ١٤٤ كلاس CSS متكرر التعريف

عدّيت الملف كامل، وطلع **١٤٤ اسم كلاس** معرّف أكتر من مرة في `globals.css` (٤٨٥٢ سطر). أبرز أمثلة:
- `.exam-plan-item`, `.exam-plan-meta` — ٩ مرات لكل واحد
- `.settings-form`, `.notification-list` — ٩ مرات
- `.result-participants` — ١١ مرة

آخر تعريف في الملف بيكسب دايمًا (CSS cascade)، فأي تعديل أو إضافة feature جديدة ممكن يكسر ستايل قديم من غير ما حد يلاحظ.

### ٥. عناوين بحجم Hero على صفحات داخلية

```css
.dashboard-header h1, .goals-header h1, .calendar-header h1 {
  font-size: clamp(2.8rem, 7vw, 6.2rem); /* لحد ٩٩ بكسل */
}
```
حجم مصمم لصفحة تسجيل الدخول/الهبوط، متسحوب على "أهلًا، [اسمك]" في الداشبورد. ده سبب مباشر لإحساس "التقل".

### ٦. لا يوجد نظام أزرار موحّد

لقيت الأنواع دي كلها موجودة بشكل منفصل، كل واحد بستايل مختلف:
`.primary-button`, `.secondary-button`, `.danger-button`, `.text-button`, `.text-button.danger`, `.timer-icon-button`, `.distraction-button`, `.compact-action`, + عشرات من `<button>` خام بلا كلاس بتاخد ستايل من الـ parent فقط (`.row-actions button`, `.goal-actions button`, `.view-tabs button`, `.segmented-control button`...). مفيش component واحد اسمه `Button` بمتغيرات (variant/size) — كل صفحة بتخترع الزرار بتاعها من الصفر تقريبًا.

### ٧. تضارب مكتبات الأيقونات

بحثت في المشروع كله: مكتبة `lucide-react` مستخدمة في **ملف واحد بس** (`app-shell.tsx` — السايدبار). كل باقي الموقع (المهام، الأهداف، الجلسات، الغرف، التقويم...) بيستخدم رموز يونيكود خام مكتوبة كنص: `↑` `↓` `→` `←` `⠿`. الرموز دي بتظهر بخط ومقاس مختلف حسب نظام تشغيل المستخدم، ومش بتاخد لون/سمك موحّد زي أيقونة SVG حقيقية، وده بيدي إحساس بصري متضارب بين السايدبار (نظيف، أيقونات SVG) والصفحات (رموز نص خام).

### ٨. زوايا الحواف (border-radius) عشوائية

فيه توكن رسمي `--radius: 18px` بالظبط لكده، لكن من أصل ~٥٠ استخدام لـ `border-radius` في الملف، **٣ بس** بيستخدموا التوكن. الباقي قيم يدوية متفرقة: `4px, 8px, 10px, 11px, 12px, 16px, 18px` — يعني الكروت والأزرار والحقول ليها زوايا مختلفة عن بعض بدون سبب تصميمي، وده بالظبط اللي بيدّي إحساس "مش موحّد".

### ٩-١٠. ألوان ومسافات بره النظام
١٨ لون hex مكتوب يدويًا (`#f7dedd`, `#95511f`...) بدل التوكنز — دول لو مش متسجلين في نظام الألوان، مش هيتغيروا صح مع الوضع الليلي. والمسافات (`gap`) بقيم زي `0.2rem, 0.3rem, 0.35rem, 0.4rem, 0.5rem, 0.6rem, 0.65rem, 0.7rem, 0.75rem, 0.8rem...` — تدرّج عشوائي بدل مقياس ثابت (زي 4/8/12/16/24/32).

---

## ليه ده كله بيحصل؟ (السبب الجذري الحقيقي)

مفيش نظام تصميم مركزي (Design System) بيتفرض على المشروع. كل صفحة/ميزة اتبنت لوحدها وكتبت الكلاسات والقيم بتاعتها من الصفر، فمع الوقت الاختلافات تراكمت وبقت متعارضة. وفولدر `src/components/ui/` (المفروض فيه المكونات المشتركة) فيه ملفين بس (`logo.tsx`, `confirm-dialog.tsx`) — يعني عمليًا مفيش "مصدر حقيقة واحد" (single source of truth) لأي عنصر UI في المشروع كله.

---

## خطة إعادة البناء الكاملة (محدّثة)

### المرحلة ٠ — إصلاح الأعطال الحرجة (يوم-يومين، قبل أي حاجة تانية)
هدف المرحلة دي: توقيف النزيف، مش تصميم جديد.
1. صلّح تحميل الخطوط: حمّل Geist فعليًا عبر `next/font/google` أو احذف الإشارة له خالص واستخدم `system-ui` كخط أساسي واضح.
2. اربط `data-theme` بالـ CSS فعليًا: استبدل `@media (prefers-color-scheme: dark)` بـ `:root[data-theme="dark"]` (+ استبقاء الـ media query كـ fallback لما `data-theme` مش موجود/System) عشان زرار الوضع الليلي يشتغل فعلاً.
3. صحّح كل استخدام لمتغير CSS غير موجود (`--text-muted` → `--muted`, `--accent-base` → `--accent`) — دوري على كل `var(--...)` في الكود وتأكدي إنه معرّف في `tokens.css`.

### المرحلة ١ — نظام تصميم موحّد (Design Tokens) — ٢-٣ أيام
1. مقياس مسافات ثابت (4 / 8 / 12 / 16 / 24 / 32 / 48px) بدل القيم العشوائية، كمتغيرات CSS.
2. مقياس زوايا موحّد (sm/md/lg/full) مبني على `--radius`، وتطبيقه بدل كل القيم اليدوية.
3. توحيد كل الألوان hex اليدوية جوه توكنز رسمية (حتى الألوان الثانوية للحالات: نجاح/تحذير/خطر).
4. حجم خط منطقي حسب سياق الصفحة: عنوان صفحة هبوط ≠ عنوان صفحة داخلية (مقياس typographic scale بـ H1/H2/H3/Body مقاسات ثابتة معقولة، مش hero-size في كل مكان).

### المرحلة ٢ — مكتبة Components مشتركة — ٣-٤ أيام
في `src/components/ui/` (بدل الملفين الحاليين):
- `Button` (variants: primary / secondary / danger / ghost / icon، sizes: sm/md/lg) — يستبدل الـ ٨ أنواع الحالية كلها.
- `Icon` wrapper مبني على `lucide-react` بس — يستبدل كل رموز اليونيكود الخام.
- `Card`, `Input`, `Select`, `Badge`, `Tabs`, `EmptyState`, `PageHeader`, `LoadingState`.

### المرحلة ٣ — الأساس البرمجي (يوازي المرحلة ٢) — ٣-٥ أيام
1. رجّع next-intl لدوره: انقل كل النصوص من الشرط اليدوي (`ar ? x : y`) إلى `messages/ar.json` / `en.json`، استخدم `useTranslations()`.
2. فعّل TanStack Query لكل نداءات API بدل تكرار `useState/useEffect/fetch` يدوي.

### المرحلة ٤ — قالب صفحة موحّد (Page Shell) — ١-٢ يوم
Component واحد `<PageShell>` للـ header/actions تستخدمه كل الصفحات، بدل كل صفحة تبني الهيكل بتاعتها.

### المرحلة ٥ — إعادة تصميم الصفحات بالترتيب
1. Dashboard
2. Tasks
3. Focus/Timer
4. Leaderboard + Challenges
5. الباقي (Settings, Goals, Exam Plans, Analytics, Calendar, Lobbies)

### المرحلة ٦ — تنظيف نهائي
- حذف كل الكلاسات المكررة (الـ ١٤٤) من `globals.css` بعد التأكد من عدم استخدامها.
- توحيد نظام إعادة ترتيب المهام (drag أو أسهم، مش الاتنين).
- مراجعة كل توحيد الأيقونات والتأكد من عدم وجود رموز يونيكود متبقية.

---

## تقدير زمني إجمالي
المرحلة ٠ (الحرجة) لوحدها ممكن تتعمل في يوم-يومين وهتحل ٣ أعطال حقيقية فورًا (الوضع الليلي، الخط، الألوان المفقودة) قبل ما نلمس أي تصميم بصري. باقي المراحل (١-٦) حوالي ١٥-٢٠ يوم عمل مركّز حسب مين هيقعد عليها.

## أقترح نبدأ منين؟
أنصح نبدأ بـ **المرحلة ٠** فورًا — دي مش تصميم، دي تصليح أعطال، وهتاخد وقت قليل جدًا مقارنة بتأثيرها (زرار الوضع الليلي هيشتغل، الخط هيتصلح، الألوان هترجع صح). بعدها نبدأ Button + PageHeader كمثال حي على صفحة Dashboard عشان تشوف شكل النظام الجديد قبل ما نكمل الباقي.
